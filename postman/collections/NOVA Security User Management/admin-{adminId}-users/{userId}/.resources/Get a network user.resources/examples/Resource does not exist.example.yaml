$kind: http-example
request:
  url: "{{baseUrl}}/admin/:adminId/users/:userId"
  method: GET
  pathVariables:
    - key: adminId
      value: elit cillum minim
      description: "(Required) "
    - key: userId
      value: elit cillum minim
      description: "(Required) "
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: The path or url does not exist
order: 4000
